apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # Uncomment after editing the host and TLS settings:
  # - ingress.yaml

images:
  - name: ghcr.io/mcphq/mcp-gateway
    newTag: latest
