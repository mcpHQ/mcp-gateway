apiVersion: v1
kind: Service
metadata:
  name: mcp-app-service
spec:
  selector:
    app: mcp-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30000
  type: NodePort